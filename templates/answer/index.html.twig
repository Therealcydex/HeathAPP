{% extends 'base.html.twig' %}

{% block title %}Answer Index{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <div class="clearfix">
                <h4 class="card-title float-left">Answer List</h4>
                <a href="{{ path('app_answer_new') }}" class="btn btn-gradient-primary float-right">
                    <i class="mdi mdi-plus"></i> Create New Answer
                </a>
            </div>

            <!-- Search Form -->
            <form method="GET" action="{{ path('app_answer_index') }}" class="mt-3">
                <div class="input-group">
                    <input type="text" name="search" value="{{ searchTerm }}" class="form-control"
                           placeholder="Search answers or questions..." aria-label="Search">
                    <div class="input-group-append">
                        <button class="btn btn-outline-primary" type="submit">Search</button>
                        {% if searchTerm %}
                            <a href="{{ path('app_answer_index') }}" class="btn btn-outline-danger">Clear</a>
                        {% endif %}
                    </div>
                </div>
            </form>

            <div class="table-responsive mt-4">
                <table class="table table-hover w-100" style="table-layout: fixed;">
                    <thead class="table-light">
                        <tr>
                            <th>
                                <a href="{{ path('app_answer_index', {'sort': 'q.text', 'order': sortOrder == 'asc' ? 'desc' : 'asc'}) }}">
                                    Question
                                </a>
                            </th>
                            <th>
                                <a href="{{ path('app_answer_index', {'sort': 'text', 'order': sortOrder == 'asc' ? 'desc' : 'asc'}) }}">
                                    Answer
                                </a>
                            </th>
                            <th>
                                <a href="{{ path('app_answer_index', {'sort': 'isCorrect', 'order': sortOrder == 'asc' ? 'desc' : 'asc'}) }}">
                                    Is Correct
                                </a>
                            </th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for answer in answers %}
                            <tr>
                                <td>{{ answer.question.text }}</td>
                                <td>{{ answer.text }}</td>
                                <td>
                                    {% if answer.isCorrect %}
                                        <span class="badge badge-success">Yes</span>
                                    {% else %}
                                        <span class="badge badge-danger">No</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <a href="{{ path('app_answer_show', {'id': answer.id}) }}" class="btn btn-info btn-sm">
                                        <i class="mdi mdi-eye"></i> Show
                                    </a>
                                    <a href="{{ path('app_answer_edit', {'id': answer.id}) }}" class="btn btn-warning btn-sm">
                                        <i class="mdi mdi-pencil"></i> Edit
                                    </a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4" class="text-center">No records found</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
